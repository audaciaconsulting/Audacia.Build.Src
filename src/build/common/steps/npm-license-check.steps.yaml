# Steps to run npm license checks 
parameters:
  - name: projectPath
    default: ''

steps:
  - script: npm install -g license-checker
    displayName: Install license checker
  - ${{ if gt(length(parameters.projectPath), 0) }}:
    - task: PowerShell@2
      displayName: Run Npm license check
      inputs:
        targetType: inline
        pwsh: true

        script: | 
          license-checker --start ${{parameters.projectPath}} --production --onlyAllow "MIT;Apache-2.0;BSD-3-Clause;Apache-2.0 OR MPL-2.0;MS-EULA;X11;UNLICENSED;0BSD;BSD-2-Clause" --excludePackages "@audacia/base-url-interceptor@1.0.0"